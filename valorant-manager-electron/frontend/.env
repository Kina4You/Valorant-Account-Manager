PUBLIC_URL=.
INLINE_RUNTIME_CHUNK=false

# Eigener Port, damit nichts mit anderen Projekten auf 3000 kollidiert.
# Steht hier statt im npm-Script, weil "PORT=3100 npm start" unter Windows
# (cmd.exe) nicht funktioniert — aus der .env liest react-scripts es überall.
PORT=3100
BROWSER=none
